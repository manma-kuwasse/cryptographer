doctype html
html(lang="ja")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title decode
        //-script 
            cont Caesar = require('./caesar.js');
            const Morse = require('./morse.js')
    body
        h1 cryptographer
        form(action="/get", method="get") 
            input(type="button" onclick="location.href='./posts';" value="home")
        a(href="./encode") encode
        img(src="./close", alt="encode")

        form(action="/get", method="get")
            textarea(name="textarea", cols="30", rows="5") 
            br
            //-input(type="button" onclick={(e) => Caesar.decode(val, key);} value="caesar")
            //-input(type="button" onclick={(e) => Morse.decode(val, keys);} value="morse")